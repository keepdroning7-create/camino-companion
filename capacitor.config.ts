import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.caminocompanion.app',
  appName: 'Camino Companion',
  webDir: 'dist'
};

export default config;
